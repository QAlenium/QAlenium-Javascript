version: 2.1

jobs:
  say-hello:
    docker:
      - image: cimg/base:stable
    steps:
      - checkout
      - run:
          name: "Say hello"
          command: "echo Hello, World!"


workflows:
  build_and_test:
    jobs:
      - build:
          filters:
            branches:
              only:
                - master